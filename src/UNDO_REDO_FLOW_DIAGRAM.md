# 🔄 Диаграмма работы системы Undo/Redo (после исправления)

## 📊 Архитектура истории

```
┌─────────────────────────────────────────────────────────────────┐
│                         History Stack                           │
│                     (MAX_HISTORY = 50)                          │
├─────────────────────────────────────────────────────────────────┤
│  Index  │  Events   │  EventZOrder  │  Projects  │              │
├─────────┼───────────┼───────────────┼────────────┼──────────────┤
│    0    │  [e1,e2]  │  Map<id,z>    │  [p1,p2]   │ ← Начальное  │
│    1    │  [e1,e2,  │  Map<id,z>    │  [p1,p2]   │ ← +Событие   │
│         │   e3]     │               │            │              │
│    2    │  [e1,e2,  │  Map<id,z>    │  [p1,p2,   │ ← +Проект    │
│         │   e3]     │               │   p3]      │              │
│    3    │  [e1,e2]  │  Map<id,z>    │  [p1,p2,   │ ← -Событие   │
│         │           │               │   p3]      │              │
│   ...   │   ...     │      ...      │    ...     │              │
└─────────┴───────────┴───────────────┴────────────┴──────────────┘
                                        ↑
                        КРИТИЧНО: Проекты ВСЕГДА присутствуют!
```

---

## 🔀 Поток данных при сохранении истории

### ✅ Правильный поток (после исправления)

```
┌──────────────────────┐
│  Пользовательское    │
│     действие         │
│  (создание события)  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  SchedulerMain.tsx   │
│                      │
│  saveHistory(        │
│    events,           │ ← events: SchedulerEvent[]
│    eventZOrder,      │ ← eventZOrder: Map<string, number>
│    projects          │ ← ✅ projects: Project[] (передаём!)
│  );                  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────┐
│  useHistory.ts → saveHistory()                           │
│                                                          │
│  if (projects.length === 0) {                           │
│    // Пытаемся взять из предыдущего состояния           │
│    const prevProjects = history[index]?.projects || []; │
│                                                          │
│    if (prevProjects.length > 0) {                       │
│      projectsToSave = prevProjects; ✅                   │
│    } else {                                              │
│      // ⚠️ КРИТИЧЕСКАЯ ПРОВЕРКА                          │
│      if (events.length > 0) {                           │
│        console.error('❌ События без проектов!');        │
│        return; // ⛔ НЕ сохраняем!                       │
│      }                                                   │
│      projectsToSave = []; // OK для начальной загрузки  │
│    }                                                     │
│  }                                                       │
│                                                          │
│  history.push({                                          │
│    events: deepCopy(events),                            │
│    eventZOrder: new Map(eventZOrder),                   │
│    projects: deepCopy(projectsToSave) ✅                │
│  });                                                     │
└──────────────────────────────────────────────────────────┘
```

### ❌ Неправильный поток (до исправления)

```
┌──────────────────────┐
│  Модалка закрыта     │
│  (Users/Projects)    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  onResetHistory()    │
│                      │
│  resetHistory(       │
│    events,           │
│    eventZOrder       │ ← ❌ projects НЕ передавались!
│  );                  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────┐
│  useHistory.ts → resetHistory()                          │
│                                                          │
│  projects = [] (по умолчанию)  ❌                        │
│                                                          │
│  history = [{                                            │
│    events: [...], ✅                                     │
│    eventZOrder: Map, ✅                                  │
│    projects: []  ❌ ← ПУСТОЙ МАССИВ!                    │
│  }];                                                     │
└──────────┬───────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────┐
│  Пользователь        │
│  нажимает Ctrl+Z     │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────┐
│  useHistory.ts → undo()                                  │
│                                                          │
│  const state = history[newIndex];                       │
│                                                          │
│  // ⚠️ ЗАЩИТА СРАБАТЫВАЕТ                                │
│  if (state.events.length > 0 &&                         │
│      state.projects.length === 0) {                     │
│    console.error('❌ События без проектов!');            │
│    return null; // ⛔ Блокируем Undo                     │
│  }                                                       │
│                                                          │
│  // Этот код НЕ выполняется ❌                           │
│  return state;                                           │
└──────────────────────────────────────────────────────────┘
```

---

## 🛡️ Двойная защита

### Уровень 1: Правильная передача параметров

```typescript
// ✅ SchedulerMain.tsx
onResetHistory={() => {
  resetHistory(events, eventZOrder, projects); // Проекты передаются!
}}
```

### Уровень 2: Валидация при сохранении

```typescript
// ✅ useHistory.ts → saveHistory()
if (events.length > 0 && projects.length === 0) {
  console.error('❌ КРИТИЧЕСКАЯ ОШИБКА!');
  return; // НЕ сохраняем некорректное состояние
}
```

### Уровень 3: Валидация при восстановлении

```typescript
// ✅ useHistory.ts → undo() / redo()
if (state.events.length > 0 && state.projects.length === 0) {
  console.error('❌ КРИТИЧЕСКАЯ ОШИБКА!');
  return null; // НЕ восстанавливаем некорректное состояние
}
```

---

## 📈 Жизненный цикл записи в истории

```
┌─────────────────┐
│  Начальная      │
│  загрузка       │
│  (isLoading)    │
└────────┬────────┘
         │
         ▼
┌──────────────────────────────────────────��─────┐
│  resetHistory(events, eventZOrder, projects)   │
│                                                │
│  history = [{                                  │
│    events: [],        ← Пока нет событий       │
│    eventZOrder: Map,                           │
│    projects: []       ← Пока нет проектов, OK  │
│  }];                                           │
│  historyIndex = 0;                             │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  Данные загружены                              │
│  events: [e1, e2], projects: [p1, p2]         │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  resetHistory(events, eventZOrder, projects)   │
│                                                │
│  history = [{                                  │
│    events: [e1, e2],  ← ✅ События             │
│    eventZOrder: Map,                           │
│    projects: [p1, p2] ← ✅ Проекты             │
│  }];                                           │
│  historyIndex = 0;                             │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  Пользователь создаёт событие e3               │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  saveHistory(                                  │
│    [e1, e2, e3],     ← Новые события           │
│    eventZOrder,                                │
│    [p1, p2]          ← ✅ Проекты передаём!    │
│  );                                            │
│                                                │
│  history = [                                   │
│    {events:[e1,e2], ..., projects:[p1,p2]},   │
│    {events:[e1,e2,e3], ..., projects:[p1,p2]} │
│  ];                                            │
│  historyIndex = 1;                             │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  Пользователь нажимает Ctrl+Z (Undo)          │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  undo()                                        │
│                                                │
│  newIndex = 0;                                 │
│  state = history[0];                          │
│                                                │
│  // Проверка                                   │
│  if (state.events.length > 0 &&               │
│      state.projects.length === 0) {           │
│    return null; ❌ Блокируем                   │
│  }                                             │
│                                                │
│  // state.projects = [p1, p2] ✅               │
│  historyIndex = 0;                             │
│  return state; ✅                              │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────┐
│  Состояние восстановлено                       │
│  events: [e1, e2]                             │
│  projects: [p1, p2]                           │
└────────────────────────────────────────────────┘
```

---

## 🔍 Ключевые моменты

### ✅ Что изменилось

1. **onResetHistory всегда передаёт projects**
   ```typescript
   // Было:
   resetHistory(events, eventZOrder);
   
   // Стало:
   resetHistory(events, eventZOrder, projects);
   ```

2. **saveHistory блокирует некорректное состояние**
   ```typescript
   if (events.length > 0 && projects.length === 0) {
     return; // НЕ сохраняем!
   }
   ```

3. **Детальное логирование**
   ```
   📝 Сброс истории после изменений (с проектами)
   📝 История: возвращаем state с 42 событиями, 12 проектами
   ```

### 🎯 Гарантии

- ✅ История НИКОГДА не содержит события без проектов
- ✅ Undo/Redo ВСЕГДА восстанавливает корректное состояние
- ✅ Защита на трёх уровнях (вызов, сохранение, восстановление)

---

**Версия**: 1.0 (2025-11-18)  
**Статус**: ✅ Исправлено и задокументировано  
**Связанные документы**: 
- `/UNDO_REDO_FIX_SUMMARY.md` - полное описание
- `/UNDO_REDO_CHEATSHEET.md` - шпаргалка для разработчиков
