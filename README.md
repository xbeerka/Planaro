
  # Planaro

  This is a code bundle for Planaro. The original project is available at https://www.figma.com/design/40hIHz0jVDB6P9N0g4zvly/Planaro.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  